import fileinput

queue = []

input = fileinput.input()
i = 0
for line in input:
  if i = 0:
    max_stack = int(line.strip())
  if i = 1:
    intial_state = line.split(";")
  if i = 2:
    end_state = line.split(";")
  i += 1
  
